# fly.toml app configuration file generated for postal-bot on 2026-07-01T14:38:31Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'postal-bot'
primary_region = 'iad'

[build]
  builder = 'paketobuildpacks/builder:base'

[processes]
  app = 'python main.py'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256
